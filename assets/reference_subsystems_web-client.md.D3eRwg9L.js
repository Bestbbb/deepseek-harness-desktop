import{_ as o,o as t,c as a,ag as r}from"./chunks/framework.DPTJCqgd.js";const h=JSON.parse('{"title":"Web Client 架构","description":"","frontmatter":{"editSource":"docs/subsystems/web-client.zh.md","editSourceUrl":"https://github.com/Bestbbb/deepseek-harness-desktop/edit/c5ef947d98383a25f1481671f55bfda8e92b1a82/docs/subsystems/web-client.zh.md","outline":[2,3]},"headers":[],"relativePath":"reference/subsystems/web-client.md","filePath":"reference/subsystems/web-client.md"}'),n={name:"reference/subsystems/web-client.md"};function c(s,e,d,i,l,p){return t(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="web-client-架构" tabindex="-1">Web Client 架构 <a class="header-anchor" href="#web-client-架构" aria-label="Permalink to &quot;Web Client 架构&quot;">​</a></h1><p>Web Client 是由独立加载插件组装而成的浏览器侧 Cordis 应用。它有四个可复用底座：<a href="./client-modules">Client Modules</a> 加载插件图，<a href="./../api-gateway">API Gateway</a> 提供类型化 Host 通信，<a href="./slots">Slots</a> 组合 React UI，<a href="./conversation">Conversation</a> 把 Session 历史窗口变成各 target 自有的视图。本文串联这些系统，并规定 Client model 与功能包各自所在的位置。</p><h2 id="分层与所有权" tabindex="-1">分层与所有权 <a class="header-anchor" href="#分层与所有权" aria-label="Permalink to &quot;分层与所有权&quot;">​</a></h2><table tabindex="0"><thead><tr><th>层</th><th>主要 owner</th><th>职责</th></tr></thead><tbody><tr><td>Host 应用</td><td>业务 service 与 <code>packages/api/*-controller</code> Host entry</td><td>拥有权威状态、持久化、mutation 顺序、访问策略与 stream 生产。</td></tr><tr><td>传输与 API assembly</td><td><code>client/connection</code>、<code>api/gateway</code>、<code>api/remotes</code></td><td>建立 Client generation，公开生成的 <code>ctx.remote</code> method 与 stream，转发选定的 Cordis event，并承载取消和结果。</td></tr><tr><td>Client model</td><td><code>api/session-controller/client</code>、<code>api/workspace-controller/client</code></td><td>维护不依赖 React 的 Host 状态镜像，处理 stream/unary 竞态，拥有对象 identity 与订阅，并公开收窄的 command service。</td></tr><tr><td>UI adapter</td><td><code>client/ui-session</code>、<code>client/ui-workspace</code></td><td>把 model observable 转换为 root 或 Session scope 的标准 Slot source，不接管业务状态所有权。</td></tr><tr><td>Conversation 数据</td><td><code>client/ui-conversation</code>、<code>ui-chat</code> 与 <code>ui-trajectory</code> 等 target package</td><td>把标准 event 与紧凑的 Assistant 历史批次组装成相互独立的 target snapshot，并拥有共享的 Conversation shell 与输入流程。</td></tr><tr><td>组合与渲染</td><td><code>client/ui-slots</code>、<code>client/ui-renderer</code>、<code>client/ui-layout</code>、各 UI 功能包</td><td>声明扩展位置、推导组件 props、把 observable 绑定成 React hook，并挂载最终组件树。</td></tr></tbody></table><p>依赖方向是 Host 状态 → Remote 传输 → Client model → UI adapter → Conversation 或 presentation → Slots → React。用户操作通过 callback 反向进入注入的 Client service 或生成的 Remote namespace。Presentation component 绝不接收 Cordis <code>ctx</code>、transport object 或其他功能插件的实现。</p><h2 id="浏览器启动" tabindex="-1">浏览器启动 <a class="header-anchor" href="#浏览器启动" aria-label="Permalink to &quot;浏览器启动&quot;">​</a></h2><p>Host 把组合后的 <code>WebBootGraph</code> 写入 <code>window.__DSH_BOOT__</code>，并在 parser-preloaded script 执行前安装浏览器 module-loader facade。模块系统是一张 lazy CommonJS 表：加载 bundle 只注册 factory；materialize entry 时才以同步 <code>require</code> 运行 factory，并解析 platform module 和已声明的动态依赖。</p><p>Web boot kernel 创建模块系统、预取 <code>immediately</code> entry、挂载 vendored Cordis Loader，再创建图中的每个 entry。Cordis service injection 决定激活顺序；module graph 顺序只决定同步 import 能否被 materialize。完整 roster 到达 settled 状态后，<code>ui-renderer</code> hydrate 不依赖框架的 boot DOM，并调用唯一一次 context 级 <code>renderSlot(&#39;root&#39;)</code>。<a href="./client-modules">Client Modules</a>负责 graph、bundle route、cache revision 与 loader 细节。</p><h2 id="remote-通信" tabindex="-1">Remote 通信 <a class="header-anchor" href="#remote-通信" aria-label="Permalink to &quot;Remote 通信&quot;">​</a></h2><p>Host 业务 service 使用 Typert Remote decorator 标记可调用 method。Host generation 产出严格 descriptor、runtime codec、declaration merge 与 source map。Client 侧 <code>api-remotes</code> assembly 选择这些生成贡献，并把具体 method 挂到 <code>ctx.remote.&lt;namespace&gt;</code> 与 Session scope 的 <code>agentCtx.remote.&lt;namespace&gt;</code>。功能包依赖生成的 service face，而不依赖 Gateway 实现或 Host 包的运行时 entry。</p><p>Connection 拥有 request correlation、<code>/api</code> carrier、trust check、精确 Fetch 路由与 connection generation。API Gateway 拥有 Remote dispatch、取消、logical stream 与选定 Host event 的转发。Controller 操作应进入生成的 Remote method 或显式 Remote stream；功能自有的下载则注册精确 Fetch 路由。<a href="./../api-gateway">API Gateway 参考</a>定义 generation 与调用，<a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/c5ef947d98383a25f1481671f55bfda8e92b1a82/packages/client/connection/README.zh.md" target="_blank" rel="noreferrer">Connection README</a>定义物理 carrier 与信任策略。</p><p>内部 <code>$events</code> logical stream 是 Connection generation source。它的 opening <code>ready</code> frame 携带用于路径显示的 Host home，并在 Host listener 已挂载、任何 controller 开始 baseline read 之前建立 generation。<code>ctx.remote.$on()</code> 把 allowlist 内的普通 event 交付给 root Client Context，并把 scoped waterfall event 交付给已解析的 Session Context；waterfall listener 可以返回结果、调用 <code>next()</code> 或拒绝。</p><h2 id="client-models" tabindex="-1">Client models <a class="header-anchor" href="#client-models" aria-label="Permalink to &quot;Client models&quot;">​</a></h2><p>每个 API controller 包都拥有配对的 Host face 与 Client face。Host 侧拥有权威 mutation 与 stream 生产；Client 侧基于相同的生成 wire type 维护 identity 稳定、与 React 无关的 model，并公开 observable snapshot 与 command。UI 包消费这些 Client service，不在 component store 中复制 transport state。</p><h3 id="sessions" tabindex="-1">Sessions <a class="header-anchor" href="#sessions" aria-label="Permalink to &quot;Sessions&quot;">​</a></h3><p><a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/c5ef947d98383a25f1481671f55bfda8e92b1a82/packages/api/session-controller/README.zh.md" target="_blank" rel="noreferrer"><code>api/session-controller</code></a>公开 Session list、search、creation、selection data、prompt、queue、cancellation、pagination 及 follow/control stream 等 Host command。其 Client 侧按 <code>ClientSessions → SessionManager → Session</code> 组织：</p><ul><li><code>ClientSessions</code> 提供 <code>ctx.sessions</code>，拥有 Session scope 与稳定的 <code>SessionBinding</code> object，并投影选中的 list state。</li><li><code>SessionManager</code> 拥有 list baseline、实时 list/control update、惰性 Session instance、queue、projection store、subagent catalog，以及 pull 与后到 update 之间的冲突顺序。</li><li>每个 <code>Session</code> 拥有一段由 <code>SessionEventLikeEntry</code> value 表示的连续逻辑 event window、pagination、follow、prompt/control state 与供 adapter 消费的 observable snapshot。</li></ul><p>持久 event 路径打开 <code>follow()</code>，其首帧包含当前 header、tail page、cursor 与完整 projection baseline。历史 record 带有显式 <code>event</code> 或 <code>chunks</code> 判别字段和字段对齐的内部 <code>event</code>；journal 先校验每条 record 的逻辑 seq 闭区间，Client 再直接把这些 record 保留为 <code>SessionEventLikeEntry</code>，无需逐 record 转换。每个物理 generation 都根据该 snapshot 原子替换保留窗口，随后按 seq append 标准实时 event。<code>page()</code> 只用于更早历史与 gap repair。瞬态 control stream 每代以完整 baseline 开始，随后应用 queue、job 与 projection update。</p><h3 id="workspaces" tabindex="-1">Workspaces <a class="header-anchor" href="#workspaces" aria-label="Permalink to &quot;Workspaces&quot;">​</a></h3><p><a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/c5ef947d98383a25f1481671f55bfda8e92b1a82/packages/api/workspace-controller/README.zh.md" target="_blank" rel="noreferrer"><code>api/workspace-controller</code></a>把 Workspace mutation policy 与权威 follow feed 留在 Host。<code>ClientWorkspaceModel</code> 拥有浏览器侧 row、order、archived Session id、command echo，以及 stream/unary 竞态合并。每代 stream 先给出完整 baseline，再给出 <code>upsert</code>、<code>remove</code>、<code>order</code> 和 <code>archived</code> increment；重连时以新 baseline 替换 model。<code>WorkspaceController</code> 把该 model 作为 <code>ctx.workspaces</code> 公开，而 <code>ui-workspace</code> 向 UI 提供 <code>useWorkspaces</code> 与 navigation callback。</p><p>这种配对不会产生第二份业务真相。Host controller 决定持久状态与 mutation outcome；Client model 维护最新可用的本地 projection，在有利于渲染时保持 object identity，并明确 delayed response 与 replacement baseline 的合并规则。</p><h2 id="conversation-与-presentation" tabindex="-1">Conversation 与 presentation <a class="header-anchor" href="#conversation-与-presentation" aria-label="Permalink to &quot;Conversation 与 presentation&quot;">​</a></h2><p><code>ui-session</code> 安装 <code>session</code> scope adapter，并提供 <code>useSessions</code>、<code>useSession</code>、<code>sessionId</code> 和 <code>useProjection</code>。领域 adapter 可以继续添加标准 source，但不会把 React hook 放进 model object。</p><p><code>ui-conversation</code> 对每个 <code>SessionBinding.eventSource</code> 只绑定一次。它的 event registry 把标准 event 与 Client-only <code>chunkrow/*</code> 历史 event 关联成稳定的业务 Context，view registry 则 materialize target snapshot。packed run 在 replay 全程保持为单个 input 与 Match；Chat Assistant、Trajectory Assistant 和 Turn Tail 是解释它的三个内建 Definition。<code>ui-chat</code> 与 <code>ui-trajectory</code> 分别注册自己的 Definition 和 builder：它们可以解释同一 event family，但不会导入或共享彼此的最终 display model。Shell 选择一个已注册 view，再通过标准 hook 与 Slot 交付其 snapshot。<a href="./conversation">Conversation</a>定义 Context identity、replay、Location data、target builder 与 keyed renderer。</p><p><code>ui-slots</code> 提供类型化 registry 与 lifecycle ledger；<code>ui-renderer</code> 是唯一通过 <code>useSyncExternalStore</code> 绑定裸 observable、拥有 React context 并渲染 root tree 的包。功能 component 通过推导出的 props 接收 framework hook、owner prop、store action 与显式 injection。<a href="./slots">Web Client Slots</a>列出这些输入、扩展 API 与当前 Slot 层级。</p><h2 id="数据通路" tabindex="-1">数据通路 <a class="header-anchor" href="#数据通路" aria-label="Permalink to &quot;数据通路&quot;">​</a></h2><table tabindex="0"><thead><tr><th>路径</th><th>顺序</th></tr></thead><tbody><tr><td>持久 Session 展示</td><td>Host Session log → packed Remote <code>follow</code>/<code>page</code> 历史 → Client <code>SessionEventLikeEntry</code> window → Conversation Context → target snapshot（<code>chat</code>、<code>trajectory</code> 或其他已注册 target）→ Slot view → React</td></tr><tr><td>瞬态 Session control</td><td>Host control baseline → Remote snapshot stream → <code>SessionManager</code> queue/job/projection store → Session 与 list snapshot → 标准 hook → component</td></tr><tr><td>Workspace 状态</td><td>Host Workspace baseline 与 increment → <code>ClientWorkspaceModel</code> → <code>ctx.workspaces.list</code> → <code>useWorkspaces</code> → sidebar、hero 与 navigation entry</td></tr><tr><td>scoped interaction</td><td>Host Cordis waterfall → API Remotes <code>$events</code> → Session Context 上的 <code>ctx.remote.$on()</code> → 所属 UI 包 → result 或 <code>next()</code></td></tr><tr><td>用户 command</td><td>component callback → 注册项 inject face 或 Slot owner → <code>ctx.sessions</code>、<code>ctx.workspaces</code> 或生成的 scoped Remote → Host Controller → 权威 update → stream 或 event projection 回到 Client</td></tr></tbody></table><h2 id="重连" tabindex="-1">重连 <a class="header-anchor" href="#重连" aria-label="Permalink to &quot;重连&quot;">​</a></h2><p>物理恢复与逻辑恢复彼此独立。Gateway mux 恢复物理 WebSocket；Connection 发布可用 generation 后，每个 <code>RemoteStream</code> 分别重开自己的 logical source。Carrier failure 可以重试；business error、非法 opening item 或 protocol violation 会令所属 logical stream 终止。</p><p>恢复方式由数据语义决定：</p><ul><li>持久 Session journal 校验逻辑 seq range，并根据每个 generation 的 opening snapshot 替换窗口；<code>page()</code> 提供更早历史并修复后续 range gap。</li><li>Session control 与 Workspace stream 在断开期间保留最后一次发布的值，再用新的 opening baseline 原子替换。</li><li>普通 forwarded notification 不会 replay。需要可靠恢复的 stateful domain 必须提供 baseline、cursor 或显式 query；scoped waterfall 保留自身的 request lifetime。</li></ul><p>架构中没有统一的 Client <code>Runtime</code>、<code>HostFrame</code>、<code>events.mux</code>、<code>events.host</code> 或通用 <code>resync()</code> API。Connection 公开 generation state，Gateway 管理 logical stream，Client model 则按自身数据定义 replacement 或 resume 语义。</p><h2 id="包边界" tabindex="-1">包边界 <a class="header-anchor" href="#包边界" aria-label="Permalink to &quot;包边界&quot;">​</a></h2><p>功能插件包可以通过 <code>import type</code> 共享声明；不得运行时导入或转发另一个功能插件的值。跨包行为使用注入的 Cordis service，跨包 UI 使用 Slots。特定 target 的 Conversation Definition、projection helper 与最终 view data 留在所属 target 包中，即使 Chat 和 Trajectory 有意实现平行逻辑。</p><p>共享运行时值需要一个职责收窄、没有功能生命周期的静态 owner，例如 <code>client/store</code>、<code>ui-primitives</code> 或浏览器安全的 util 包。Transport 与生成 API assembly 可以导入运行时 contribution，因为组装同一个 protocol 正是它们的显式职责。功能包不能只为绕过此规则而添加 <code>dsh.client.external</code>。</p><p>根据所添加的扩展查阅四篇详细参考：</p><ul><li><a href="./client-modules">Client Modules</a>：package discovery、loading、共享 module identity 与 boot order。</li><li><a href="./../api-gateway">API Gateway</a>：Host method、生成的 Remote contribution、stream 与 forwarded event。</li><li><a href="./slots">Web Client Slots</a>：component、hook、store、injection 与 placement。</li><li><a href="./conversation">Conversation</a>：持久 event correlation、target snapshot，以及 Chat 或 Trajectory view contribution。</li></ul>',37)])])}const u=o(n,[["render",c]]);export{h as __pageData,u as default};
