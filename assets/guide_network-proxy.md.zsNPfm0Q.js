import{_ as s,o as a,c as t,ag as o}from"./chunks/framework.DPTJCqgd.js";const k=JSON.parse('{"title":"在网络代理后面运行 DSH","description":"","frontmatter":{"editSource":"docs/user/guide/network-proxy.zh.md","editSourceUrl":"https://github.com/Bestbbb/deepseek-harness-desktop/edit/2847c75ea844b05f9d8adca865940856f1286c8c/docs/user/guide/network-proxy.zh.md"},"headers":[],"relativePath":"guide/network-proxy.md","filePath":"guide/network-proxy.md"}'),i={name:"guide/network-proxy.md"};function d(p,e,h,c,l,n){return a(),t("div",null,[...e[0]||(e[0]=[o(`<h1 id="在网络代理后面运行-dsh" tabindex="-1">在网络代理后面运行 DSH <a class="header-anchor" href="#在网络代理后面运行-dsh" aria-label="Permalink to &quot;在网络代理后面运行 DSH&quot;">​</a></h1><p>DSH 会把自身的出站请求——模型调用、web 搜索、页面抓取、走 HTTP 的 MCP 服务器——都经由标准代理环境变量所指定的代理发出。它在启动时读取这些变量，不需要其他配置。有几条路径出于设计或运行时限制保持直连，下文&quot;哪些保持直连&quot;一节列出了它们。</p><h2 id="导出环境变量" tabindex="-1">导出环境变量 <a class="header-anchor" href="#导出环境变量" aria-label="Permalink to &quot;导出环境变量&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HTTPS_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://127.0.0.1:7890</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HTTP_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">http://127.0.0.1:7890</span></span></code></pre></div><p>把这两行写进 shell 配置，这样每次调用 <code>dsh</code> 都会继承它们；也可以写进 <code>$DSH_HOME/.env</code>（默认 <code>~/.dsh/.env</code>），和 API key 放在一起；导出的环境变量始终优先于该文件。项目自己的 <code>.env</code> 不能设置它们：它随 <code>git clone</code> 一起到来，DSH 宁可拒绝启动，也不让一个仓库决定你的流量去向。</p><p>需要凭据的代理把凭据写在 URL 里：<code>http://user:password@proxy.example:8080</code>。DSH 绝不会回显这个 URL：诊断只点名被拒绝的变量，因此用户名和密码都不会出现在任何地方。</p><h2 id="为什么浏览器走代理、终端却不走" tabindex="-1">为什么浏览器走代理、终端却不走 <a class="header-anchor" href="#为什么浏览器走代理、终端却不走" aria-label="Permalink to &quot;为什么浏览器走代理、终端却不走&quot;">​</a></h2><p>这是最常见的意外，而且并非 DSH 特有。<strong>根本不存在一个所有软件都遵循的&quot;系统代理&quot;</strong>——实际上有三套互不相干的机制：</p><table tabindex="0"><thead><tr><th>机制</th><th>谁会遵循</th></tr></thead><tbody><tr><td>操作系统的代理设置</td><td>Safari、绝大多数 macOS 原生应用、Chrome 与 Edge</td></tr><tr><td><code>HTTP_PROXY</code> / <code>HTTPS_PROXY</code> 环境变量</td><td><code>curl</code>、<code>git</code>、<code>npm</code>、<code>pip</code> 以及 DSH</td></tr><tr><td>TUN 模式（虚拟网卡）</td><td>所有程序，且对应用透明</td></tr></tbody></table><p>Clash 这类代理软件里的&quot;系统代理&quot;开关只写第一套。浏览器会读到它，命令行工具则永远看不到。这就是为什么导出环境变量是一个独立步骤，也是为什么打开 TUN 模式后两者都能工作、且完全不需要变量。</p><p>DSH 不读取操作系统的代理设置。请导出环境变量，或使用 TUN 模式。</p><h2 id="指定哪些目标保持直连" tabindex="-1">指定哪些目标保持直连 <a class="header-anchor" href="#指定哪些目标保持直连" aria-label="Permalink to &quot;指定哪些目标保持直连&quot;">​</a></h2><p><code>NO_PROXY</code> 列出需要直连的主机：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NO_PROXY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">internal.example.com,.corp.example.com,registry.local</span></span></code></pre></div><p>一个条目写的是主机名，它连同其下所有子域名一起匹配：<code>NO_PROXY=example.com</code> 也会让 <code>api.example.com</code> 直连。前缀 <code>.</code> 或 <code>*.</code> 可以写，含义相同。条目可带 <code>:port</code>，<code>*</code> 则放行全部。</p><p><strong>CIDR 网段不生效。</strong> 操作系统的绕过列表常含 <code>10.0.0.0/8</code> 或 <code>192.168.0.0/16</code> 这类条目；把它们复制进 <code>NO_PROXY</code> 不会有任何效果。请改用主机名或域名后缀。</p><p>不需要列出 <code>localhost</code> 或 <code>127.0.0.1</code>。DSH 始终绕过 loopback，否则它自己的 Web UI 与本地服务器都会经由代理并形成回环。</p><h2 id="值得知道的限制" tabindex="-1">值得知道的限制 <a class="header-anchor" href="#值得知道的限制" aria-label="Permalink to &quot;值得知道的限制&quot;">​</a></h2><p><strong>不支持 SOCKS 代理。</strong> <code>socks5://</code> 形式的值会在启动时被报告并跳过，指定它的那个 scheme 转为直连——把 <code>HTTPS_PROXY=socks5://…</code> 与一个可用的 <code>HTTP_PROXY</code> 一起设置时，<code>https:</code> 会保持直连，而不会去借用 HTTP 代理。请把变量指向代理软件的 HTTP 端口——多数软件两者都提供，且 HTTP 端口通常就在相邻的端口号上。</p><p><strong>只设 <code>ALL_PROXY</code> 也够用。</strong> DSH 会用它为两种协议兜底，尽管 Node 与 curl 在这一点上并不一致。显式设置 <code>HTTPS_PROXY</code> 仍然更清楚。</p><p><strong>做 TLS 拦截的企业代理需要它的证书。</strong> 如果代理已经可达但请求仍报证书错误，请在启动前把 Node 指向你所在组织的 CA 包：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> NODE_EXTRA_CA_CERTS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/path/to/corporate-ca.pem</span></span></code></pre></div><p>Node 只在进程启动时读取该变量，所以要在运行 <code>dsh</code> 之前导出。</p><p><strong>DSH 替你运行的工具遵循同一个代理。</strong> bash 工具里的命令、<code>git</code>、<code>gh</code>，以及作为子进程启动的 MCP 服务器都会继承这些变量。子进程若本身是 Node 程序，则需 Node 22.21 或更高版本才会遵循；更旧的 Node 会直连。如果你的某个代理变量是 DSH 拒绝的值——比如 SOCKS URL——基于 Node 的工具同样直连而不是起不来，<code>curl</code> 与 <code>git</code> 则仍会读取那个值。</p><p><strong>代理 URL 里的密码同样会到达这些工具。</strong> <code>HTTPS_PROXY=http://alice:s3cret@proxy.example:8080</code> 就是一个普通环境变量，因此 DSH 运行的每一条命令——包括模型编写的那些——都能读到它，而打印环境的命令会把密码写进被保留的输出。这与该变量在你 shell 里对其他一切程序的行为一致。若这一点重要，请为代理提供一个无需凭据的入口，或改用 URL 之外的方式认证。</p><h2 id="哪些保持直连" tabindex="-1">哪些保持直连 <a class="header-anchor" href="#哪些保持直连" aria-label="Permalink to &quot;哪些保持直连&quot;">​</a></h2><p>并非 DSH 发出的每个请求都会走代理：</p><ul><li><strong>本机上的一切。</strong> loopback 始终直连：<code>localhost</code>、整个 <code>127.0.0.0/8</code> 段、<code>::1</code> 与 <code>0.0.0.0</code>。代理无法有意义地访问一个只在本地监听的服务。</li><li><strong>模型编写的代码。</strong> workflow 与 code-runtime worker 从不接收代理配置，因此模型编写的脚本读不到可能携带密码的代理 URL。这类脚本只有自行配置才能联网。</li><li><strong>使用情况遥测。</strong> OTLP 导出器用的是 Node 自带的 HTTP 客户端，而不是代理所配置的那个，因此遥测直连；在禁止直连出网的环境里它只会失败。DSH 的任何功能都不依赖它。设 <code>DSH_TELEMETRY_MODE=DISABLED</code> 可完全关闭。</li><li><strong><code>web_fetch</code> 访问字面量私网地址。</strong> 形如 <code>http://10.0.0.5/</code> 的 URL 会被拒绝而非交给代理，与未配置代理时得到的拒绝相同。</li></ul><h2 id="验证是否生效" tabindex="-1">验证是否生效 <a class="header-anchor" href="#验证是否生效" aria-label="Permalink to &quot;验证是否生效&quot;">​</a></h2><p>让 agent 抓取一个页面，同时观察代理软件的连接日志：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">dsh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --profile</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> headless</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;fetch https://example.com and tell me the page title&quot;</span></span></code></pre></div><p>如果请求没有出现在那里，确认变量确实进入了 DSH 自己的环境：</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">env</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> grep</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> proxy</span></span></code></pre></div>`,33)])])}const g=s(i,[["render",d]]);export{k as __pageData,g as default};
