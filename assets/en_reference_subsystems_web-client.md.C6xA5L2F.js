import{_ as t,o,c as a,ag as s}from"./chunks/framework.DPTJCqgd.js";const u=JSON.parse('{"title":"Web Client architecture","description":"","frontmatter":{"editSource":"docs/subsystems/web-client.md","editSourceUrl":"https://github.com/Bestbbb/deepseek-harness-desktop/edit/2847c75ea844b05f9d8adca865940856f1286c8c/docs/subsystems/web-client.md","outline":[2,3]},"headers":[],"relativePath":"en/reference/subsystems/web-client.md","filePath":"en/reference/subsystems/web-client.md"}'),n={name:"en/reference/subsystems/web-client.md"};function r(i,e,d,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="web-client-architecture" tabindex="-1">Web Client architecture <a class="header-anchor" href="#web-client-architecture" aria-label="Permalink to &quot;Web Client architecture&quot;">​</a></h1><p>The Web Client is a browser-side Cordis application assembled from independently loaded plugins. Its architecture has four reusable foundations: <a href="./client-modules">Client Modules</a> loads the plugin graph, the <a href="./../api-gateway">API Gateway</a> provides typed Host communication, <a href="./slots">Slots</a> composes React UI, and <a href="./conversation">Conversation</a> turns a Session history window into target-owned views. This page connects those systems and defines where Client models and feature packages belong.</p><h2 id="layers-and-ownership" tabindex="-1">Layers and ownership <a class="header-anchor" href="#layers-and-ownership" aria-label="Permalink to &quot;Layers and ownership&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Main owners</th><th>Responsibility</th></tr></thead><tbody><tr><td>Host application</td><td>business services and <code>packages/api/*-controller</code> Host entries</td><td>Own authoritative state, persistence, mutation ordering, access policy, and stream production.</td></tr><tr><td>Transport and API assembly</td><td><code>client/connection</code>, <code>api/gateway</code>, <code>api/remotes</code></td><td>Establish a Client generation, expose generated <code>ctx.remote</code> methods and streams, forward selected Cordis events, and carry cancellation and results.</td></tr><tr><td>Client models</td><td><code>api/session-controller/client</code>, <code>api/workspace-controller/client</code></td><td>Maintain React-free mirrors of Host state, resolve stream/unary races, own object identities and subscriptions, and expose narrow command services.</td></tr><tr><td>UI adapters</td><td><code>client/ui-session</code>, <code>client/ui-workspace</code></td><td>Convert model observables into root or Session-scoped standard Slot sources without taking ownership of business state.</td></tr><tr><td>Conversation data</td><td><code>client/ui-conversation</code>, target packages such as <code>ui-chat</code> and <code>ui-trajectory</code></td><td>Assemble standard events and compact historical Assistant runs into independent target snapshots and own the shared conversation shell and input flow.</td></tr><tr><td>Composition and rendering</td><td><code>client/ui-slots</code>, <code>client/ui-renderer</code>, <code>client/ui-layout</code>, feature UI packages</td><td>Declare extension locations, derive component props, bind observables to React hooks, and mount the final tree.</td></tr></tbody></table><p>The dependency direction is Host state → Remote transport → Client model → UI adapter → Conversation or presentation → Slots → React. User actions travel back through callbacks that close over an injected Client service or generated Remote namespace. A presentation component never receives Cordis <code>ctx</code>, a transport object, or another feature plugin&#39;s implementation.</p><h2 id="browser-boot" tabindex="-1">Browser boot <a class="header-anchor" href="#browser-boot" aria-label="Permalink to &quot;Browser boot&quot;">​</a></h2><p>The Host writes the composed <code>WebBootGraph</code> to <code>window.__DSH_BOOT__</code> and installs the browser module-loader facade before parser-preloaded scripts execute. The module system is a lazy CommonJS table: loading a bundle registers its factory, while materializing an entry runs the factory with synchronous <code>require</code> over platform modules and declared dynamic dependencies.</p><p>The Web boot kernel creates the module system, prefetches <code>immediately</code> entries, mounts the vendored Cordis Loader, and creates every graph entry. Cordis service injection determines activation; module graph order determines only whether synchronous imports can be materialized. After the complete roster reaches a settled state, <code>ui-renderer</code> hydrates the framework-free boot DOM and calls the sole context-level <code>renderSlot(&#39;root&#39;)</code> operation. <a href="./client-modules">Client Modules</a> owns the graph, bundle route, cache revision, and loader details.</p><h2 id="remote-communication" tabindex="-1">Remote communication <a class="header-anchor" href="#remote-communication" aria-label="Permalink to &quot;Remote communication&quot;">​</a></h2><p>Host business services annotate callable methods with Typert Remote decorators. Host generation emits strict descriptors, runtime codecs, declaration merges, and source maps. The Client-side <code>api-remotes</code> assembly selects those generated contributions and mounts concrete methods under <code>ctx.remote.&lt;namespace&gt;</code> and Session-scoped <code>agentCtx.remote.&lt;namespace&gt;</code>. Feature packages depend on the generated service face, not the Gateway implementation or a Host package&#39;s runtime entry.</p><p>The Connection owns request correlation, the <code>/api</code> carrier, trust checks, exact Fetch routes, and connection generations. API Gateway owns Remote dispatch, cancellation, logical streams, and selected Host event forwarding. Controller operations belong on generated Remote methods or explicit Remote streams; feature-owned downloads register exact Fetch routes. The <a href="./../api-gateway">API Gateway reference</a> defines generation and invocation, while the <a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/2847c75ea844b05f9d8adca865940856f1286c8c/packages/client/connection/README.md" target="_blank" rel="noreferrer">Connection README</a> defines the physical carrier and trust policy.</p><p>The internal <code>$events</code> logical stream is the Connection generation source. Its opening <code>ready</code> frame carries the Host home used for path display and establishes the generation after Host listeners are attached, before any controller begins a baseline read. <code>ctx.remote.$on()</code> delivers allowlisted ordinary events to the root Client Context and scoped waterfall events to the resolved Session Context; a waterfall listener returns a result, calls <code>next()</code>, or rejects.</p><h2 id="client-models" tabindex="-1">Client models <a class="header-anchor" href="#client-models" aria-label="Permalink to &quot;Client models&quot;">​</a></h2><p>Each API controller package owns a paired Host and Client face. The Host side owns authoritative mutation and stream production. The Client side owns an identity-stable, React-free model over the same generated wire types and exposes observable snapshots plus commands. UI packages consume these Client services and do not reproduce transport state in component stores.</p><h3 id="sessions" tabindex="-1">Sessions <a class="header-anchor" href="#sessions" aria-label="Permalink to &quot;Sessions&quot;">​</a></h3><p><a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/2847c75ea844b05f9d8adca865940856f1286c8c/packages/api/session-controller/README.md" target="_blank" rel="noreferrer"><code>api/session-controller</code></a> exposes Host commands for list, search, creation, selection data, prompt, queue, cancellation, pagination, and follow/control streams. Its Client side is organized as <code>ClientSessions → SessionManager → Session</code>:</p><ul><li><code>ClientSessions</code> provides <code>ctx.sessions</code>, owns Session scopes and stable <code>SessionBinding</code> objects, and projects the selected list state.</li><li><code>SessionManager</code> owns the list baseline, live list/control updates, lazy Session instances, queues, projection stores, subagent catalogs, and conflict ordering between pulls and later updates.</li><li>Each <code>Session</code> owns one contiguous logical-event window represented by <code>SessionEventLikeEntry</code> values, paging, follow, prompt/control state, and the observable snapshot consumed by adapters.</li></ul><p>The durable event path opens <code>follow()</code>, whose first frame contains the current header, tail page, cursor, and complete projection baseline. History records have an explicit <code>event</code> or <code>chunks</code> discriminator and an aligned inner <code>event</code>; the journal validates each inclusive logical sequence range before the Client retains the records as <code>SessionEventLikeEntry</code> values without per-record conversion. Each physical generation atomically replaces the retained window from that snapshot; standard live events then append by sequence. <code>page()</code> is reserved for older history and gap repair. The transient control stream starts every generation with a complete baseline and then applies queue, job, and projection updates.</p><h3 id="workspaces" tabindex="-1">Workspaces <a class="header-anchor" href="#workspaces" aria-label="Permalink to &quot;Workspaces&quot;">​</a></h3><p><a href="https://github.com/Bestbbb/deepseek-harness-desktop/blob/2847c75ea844b05f9d8adca865940856f1286c8c/packages/api/workspace-controller/README.md" target="_blank" rel="noreferrer"><code>api/workspace-controller</code></a> keeps Workspace mutation policy and the authoritative follow feed on the Host. <code>ClientWorkspaceModel</code> owns the browser rows, order, archived Session ids, command echoes, and stream/unary race resolution. Every stream generation starts with a complete baseline followed by <code>upsert</code>, <code>remove</code>, <code>order</code>, and <code>archived</code> increments; reconnect replaces the model from the new baseline. <code>WorkspaceController</code> exposes that model as <code>ctx.workspaces</code>, while <code>ui-workspace</code> contributes <code>useWorkspaces</code> and navigation callbacks to the UI.</p><p>This pairing is not a second source of business truth. Host controllers decide durable state and mutation outcomes; Client models maintain the latest usable local projection, preserve object identity where useful to rendering, and encode how delayed responses and replacement baselines merge.</p><h2 id="conversation-and-presentation" tabindex="-1">Conversation and presentation <a class="header-anchor" href="#conversation-and-presentation" aria-label="Permalink to &quot;Conversation and presentation&quot;">​</a></h2><p><code>ui-session</code> installs the <code>session</code> scope adapter and publishes <code>useSessions</code>, <code>useSession</code>, <code>sessionId</code>, and <code>useProjection</code>. Domain adapters add further standard sources without putting React hooks on the model objects.</p><p><code>ui-conversation</code> binds once to each <code>SessionBinding.eventSource</code>. Its event registry correlates durable Session events and Client-only <code>assistant/live-chunk</code> updates into stable business Contexts, and its view registry materializes target snapshots. Chat Assistant, Trajectory Assistant, and Turn Tail interpret both live chunks and the compact streams embedded in durable settlements, so reconnect and paged history reproduce the same Assistant state without durable token rows. <code>ui-chat</code> and <code>ui-trajectory</code> register separate Definitions and builders: they may interpret the same event family, but they do not import or share each other&#39;s final display model. The shell selects a registered view and passes its snapshot through standard hooks and Slots. <a href="./conversation">Conversation</a> defines Context identity, replay, Location data, target builders, and keyed renderers.</p><p><code>ui-slots</code> provides the typed registry and lifecycle ledger; <code>ui-renderer</code> is the only package that binds bare observables through <code>useSyncExternalStore</code>, owns React contexts, and renders the root tree. Feature components receive framework hooks, owner props, store actions, and explicit injection through their derived props. <a href="./slots">Web Client Slots</a> lists those inputs, extension APIs, and the current Slot hierarchy.</p><h2 id="data-paths" tabindex="-1">Data paths <a class="header-anchor" href="#data-paths" aria-label="Permalink to &quot;Data paths&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Path</th><th>Sequence</th></tr></thead><tbody><tr><td>durable Session display</td><td>Host Session log → packed Remote <code>follow</code>/<code>page</code> history → Client <code>SessionEventLikeEntry</code> window → Conversation Contexts → target snapshot (<code>chat</code>, <code>trajectory</code>, or another registered target) → Slot view → React</td></tr><tr><td>transient Session control</td><td>Host control baseline → Remote snapshot stream → <code>SessionManager</code> queue/job/projection stores → Session and list snapshots → standard hooks → components</td></tr><tr><td>Workspace state</td><td>Host Workspace baseline and increments → <code>ClientWorkspaceModel</code> → <code>ctx.workspaces.list</code> → <code>useWorkspaces</code> → sidebar, hero, and navigation entries</td></tr><tr><td>scoped interaction</td><td>Host Cordis waterfall → API Remotes <code>$events</code> → <code>ctx.remote.$on()</code> on the Session Context → owning UI package → result or <code>next()</code></td></tr><tr><td>user command</td><td>component callback → registration inject face or Slot owner → <code>ctx.sessions</code>, <code>ctx.workspaces</code>, or generated scoped Remote → Host Controller → authoritative update → stream or event projection back to the Client</td></tr></tbody></table><h2 id="reconnection" tabindex="-1">Reconnection <a class="header-anchor" href="#reconnection" aria-label="Permalink to &quot;Reconnection&quot;">​</a></h2><p>Physical and logical recovery are separate. Gateway mux restores the physical WebSocket; each <code>RemoteStream</code> reopens its own logical source when the Connection publishes a usable generation. A carrier failure is retryable, while a business error, malformed opening item, or protocol violation is terminal for the owning logical stream.</p><p>Recovery follows the data&#39;s semantics:</p><ul><li>A durable Session journal validates logical sequence ranges and replaces its window from every generation&#39;s opening snapshot; <code>page()</code> supplies older history and repairs any later range gap.</li><li>Session control and Workspace streams retain the last published value while disconnected, then atomically replace it from a fresh opening baseline.</li><li>Ordinary forwarded notifications are not replayed. Stateful domains need a baseline, cursor, or explicit query; scoped waterfalls retain their own request lifetime.</li></ul><p>There is no monolithic Client <code>Runtime</code>, <code>HostFrame</code>, <code>events.mux</code>, <code>events.host</code>, or universal <code>resync()</code> API. The Connection exposes generation state, Gateway owns logical stream supervision, and each Client model defines replacement or resume semantics appropriate to its data.</p><h2 id="package-boundaries" tabindex="-1">Package boundaries <a class="header-anchor" href="#package-boundaries" aria-label="Permalink to &quot;Package boundaries&quot;">​</a></h2><p>Feature plugin packages may share declarations through <code>import type</code>; they do not runtime-import or re-export another feature plugin&#39;s values. Cross-package behavior uses injected Cordis services, and cross-package UI uses Slots. Target-specific Conversation Definitions, projection helpers, and final view data stay with their target package even when Chat and Trajectory intentionally implement parallel logic.</p><p>Shared runtime values need a narrow static owner with no feature lifecycle, such as <code>client/store</code>, <code>ui-primitives</code>, or a browser-safe utility package. Transport and generated API assembly may import runtime contributions because assembling one protocol is their explicit responsibility. A feature package does not add <code>dsh.client.external</code> merely to bypass this rule.</p><p>Use the four detailed references according to the extension being added:</p><ul><li><a href="./client-modules">Client Modules</a> for package discovery, loading, shared module identities, and boot order.</li><li><a href="./../api-gateway">API Gateway</a> for Host methods, generated Remote contributions, streams, and forwarded events.</li><li><a href="./slots">Web Client Slots</a> for components, hooks, stores, injection, and placement.</li><li><a href="./conversation">Conversation</a> for durable event correlation, target snapshots, and Chat or Trajectory view contributions.</li></ul>',37)])])}const m=t(n,[["render",r]]);export{u as __pageData,m as default};
