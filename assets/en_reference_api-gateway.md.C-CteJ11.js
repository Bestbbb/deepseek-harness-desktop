import{_ as s,o as t,c as i,ag as a}from"./chunks/framework.DPTJCqgd.js";const k=JSON.parse('{"title":"API Gateway","description":"","frontmatter":{"editSource":"docs/api-gateway.md","editSourceUrl":"https://github.com/Bestbbb/deepseek-harness-desktop/edit/c5ef947d98383a25f1481671f55bfda8e92b1a82/docs/api-gateway.md"},"headers":[],"relativePath":"en/reference/api-gateway.md","filePath":"en/reference/api-gateway.md"}'),n={name:"en/reference/api-gateway.md"};function o(r,e,d,l,p,h){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="api-gateway" tabindex="-1">API Gateway <a class="header-anchor" href="#api-gateway" aria-label="Permalink to &quot;API Gateway&quot;">​</a></h1><p>This is the current-state reference for the Typert API Gateway. It describes how business services declare unary Remote methods, how the build generates Host and Client contracts, and how calls reuse the Connection RPC and <code>/api</code> route. Session events, incremental data, and other streaming protocols are outside this document&#39;s scope; they may use the same Connection but do not use Remote method descriptors.</p><h2 id="programming-model" tabindex="-1">Programming model <a class="header-anchor" href="#programming-model" aria-label="Permalink to &quot;Programming model&quot;">​</a></h2><p>Business services use <code>@Remote</code> or <code>@RemoteScope</code> to select the methods exposed to the Client. Unmarked methods do not enter the generated Client types or runtime contributions and cannot be called through <code>ctx.remote</code>.</p><p><code>@Remote</code> denotes calling a Cordis service registered on the root Host Context. Complex Host objects cannot cross the wire directly; the business package must declare their association with a wire identity through <code>TypertLookupMap</code> and register a default resolution provider with <code>ctx.typert.lookups</code> at runtime. For example, an <code>Agent</code> parameter named <code>agent</code> in the Host signature produces an <code>agentId</code> wire field, and the Gateway resolves that id to a Host object before invoking the business method. Host composition can use <code>ctx.typert.lookups.configure()</code> to override the resolution policy for a lookup key without changing the parameter name, wire field, or canonical type symbol owned by the business package.</p><p><code>@RemoteScope(key)</code> first resolves an identity to a scoped Context through <code>ctx.typert.contexts</code>, then obtains the service from that Context and invokes the method. It applies when the method itself depends on scoped composition and does not need to receive objects such as <code>Agent</code> explicitly.</p><p>Services normally extend <code>TypertRemoteService</code> so the constructor explicitly binds the Cordis service key and default Remote namespace. A service that already has another base class can instead declare <code>readonly typertRemote = bindTypertRemote(this, serviceKey)</code>; both forms leave an inspectable public binding and do not depend on the compiler injecting a symbol into the constructor.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Agent } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/dsh-agent&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { TypertRemoteService, Remote, RemoteScope } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/dsh-typert-protocol&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Context } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/cordis&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  objective</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  accepted</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GoalService</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> extends</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypertRemoteService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  constructor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Context</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    super</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;goals&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Remote</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;create&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  createForClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    agent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    signal</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AbortSignal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  )</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    signal.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">throwIfAborted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> this</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(agent, request)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  @</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RemoteScope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;agent&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;current&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  currentForClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { accepted: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  private</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">_agent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Agent</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">request</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalRequest</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> CreateGoalResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { accepted: request.objective.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Remote methods may return a value synchronously or return a Promise. For cooperative cancellation, the final parameter in the Host signature must be <code>signal: AbortSignal</code> using the global type; it is recorded in the descriptor instead of entering <code>args</code>, while the generated Client method accepts an optional final <code>AbortSignal</code>.</p><p>The Client uses concrete functions on ordinary objects, not a JavaScript Proxy. Direct and scoped calls appear under <code>ctx.remote.&lt;namespace&gt;</code> and <code>agentCtx.remote.&lt;namespace&gt;</code>. Each namespace is a traced Cordis child Service registered as <code>remote.&lt;namespace&gt;</code>; the Client assembly mounts contributions through <code>ctx.remote.$mount()</code>, and the namespace unloads after its last method is withdrawn. Dependency declarations belong to the actual caller: only a business package that reads <code>ctx.remote.&lt;namespace&gt;</code> or <code>agentCtx.remote.&lt;namespace&gt;</code> declares both <code>remote</code> and <code>remote.&lt;namespace&gt;</code> in its own <code>inject</code>; assemblies that only mount contributions and higher-level runtimes that do not call that namespace do not declare the namespace dependency on the business package&#39;s behalf. When an <code>@Remote</code> method has exactly one lookup parameter and a same-named <code>TypertContextMap</code> uses the same wire identity, the generated scoped signature omits that identity parameter. <code>@RemoteScope</code> generates only the scoped invocation interface.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { SessionId } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/dsh-session/types&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { AgentContext } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/dsh-api-session-controller/client&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Context } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/cordis&#39;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@deepseek-ai/dsh-api-remotes/client&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> inject</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;remote&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;remote.goals&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ctx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Context</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> agentCtx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AgentContext</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">declare</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> agentId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SessionId</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ctx.remote.goals.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(agentId, { objective: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ship it&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> agentCtx.remote.goals.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ objective: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ship it&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> })</span></span></code></pre></div><p>Client applications assemble only <code>@deepseek-ai/dsh-api-remotes</code>. That package imports the <code>/remote</code> subpaths of selected business packages as runtime values, mounts their contributions through <code>ctx.remote.$mount()</code>, and re-exports the declaration merges from the same files. Adding a Host Remote package is an explicit choice by the Client composition owner; business components do not need to load the Typert Gateway or the business package&#39;s Remote JS separately.</p><p>The <code>api-remotes</code> assembly and the <code>ctx.remote</code> contract are React-independent; the Host methods visible to any Client assembly are limited to the Remote methods selected at generation time.</p><h2 id="component-responsibilities" tabindex="-1">Component responsibilities <a class="header-anchor" href="#component-responsibilities" aria-label="Permalink to &quot;Component responsibilities&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Location</th><th>Package or entry</th><th>Responsibility</th></tr></thead><tbody><tr><td>Shared</td><td><code>@deepseek-ai/dsh-typert-protocol</code></td><td>Declares decorators, Gateway bindings, merge-extensible protocol maps, invocation descriptors, and provider types; starts no TypeScript analysis and registers no Cordis services</td></tr><tr><td>Build</td><td><code>@deepseek-ai/dsh-typert-generator</code></td><td>Strictly analyzes Remote signatures, the type graph, lookups, Contexts, and source locations from the Host <code>ts.Program</code>, then generates Host and Host-for-Client artifacts</td></tr><tr><td>Host</td><td><code>@deepseek-ai/dsh-typert-registry</code> and Loader</td><td>Places generated Host descriptors, schemas, and business-package registrations in <code>ctx.typert</code>, and holds lookup and Context providers</td></tr><tr><td>Host</td><td><code>@deepseek-ai/dsh-api-session-controller</code></td><td>Owns the application Agent/Session identity policy and configures the corresponding Typert lookups</td></tr><tr><td>Host</td><td><code>@deepseek-ai/dsh-api-gateway</code></td><td>Provides <code>ctx.typertGateway</code>, claims Remote endpoints, resolves objects or Contexts, invokes live Cordis services, and validates request and return values</td></tr><tr><td>Client</td><td><code>@deepseek-ai/dsh-api-gateway/client</code></td><td>Provides <code>ctx.remote</code> and <code>remote.&lt;namespace&gt;</code> child Services, mounts generated descriptors as concrete methods, and initiates, validates, and cancels calls through the Connection</td></tr><tr><td>Client</td><td><code>@deepseek-ai/dsh-api-remotes/client</code></td><td>Explicitly selects and mounts the <code>/remote</code> contributions allowed by the application and brings the corresponding declaration merges into business code</td></tr><tr><td>Both</td><td><code>@deepseek-ai/dsh-client-connection</code></td><td>Provides the RPC carrier, request correlation, trust boundary, cancellation, response envelope, and the <code>/api</code> HTTP bridge</td></tr></tbody></table><p>The API Gateway package owns the Host dispatcher and Client Remote endpoint as peer entries, but the two builds never enter the same <code>ts.Program</code>. The Host entry does not import the Client Cordis <code>Context</code> merge, and the Client entry does not import the Host Gateway service.</p><h2 id="strict-generation-pipeline" tabindex="-1">Strict generation pipeline <a class="header-anchor" href="#strict-generation-pipeline" aria-label="Permalink to &quot;Strict generation pipeline&quot;">​</a></h2><p>The root build runs <code>build:lib:host</code>, <code>build:lib:client</code>, and <code>build:web</code> in order. The Host lib phase first runs <code>tsc -b tsconfig.host.json</code>, then <code>tsdown --env.DSH_BUILD_FACE host</code>; the normal Host Project Reference graph compiles the Typert generator, which runs during this tsdown pass with the Host aggregate as its only <code>ts.Program</code> seed. The Client lib phase then runs <code>tsc -b tsconfig.client.json</code> and <code>tsdown --env.DSH_BUILD_FACE client</code>, consuming the newly generated Remote Client declarations and runtime contributions without starting Typert again.</p><p>Both tsdown passes receive the complete workspace and bundle only JavaScript emitted to <code>lib/types</code> by the corresponding tsc phase. The root config does not scan Client artifacts, classify package names, or pass a maintained filter to tsdown; package-local configs return entries for the current phase based on <code>DSH_BUILD_FACE</code>. An ordinary Client plugin produces both its Node loader entry and browser bundle during the Client phase.</p><p><code>api/remotes</code>, <code>api/gateway</code>, <code>api/session-controller</code>, and <code>api/workspace-controller</code> (plus <code>client/connection</code>) split TypeScript faces. <code>api/remotes</code>&#39; Client project depends on <code>/remote</code> declarations generated for business packages during Host tsdown; root aggregates and direct consumers must reference each split package&#39;s <code>tsconfig.host.json</code> or <code>tsconfig.client.json</code> respectively. <code>api-remotes</code>&#39; <code>clientBundle(..., { hostPhase: true })</code> produces its Host entry during Host tsdown and leaves only the browser entry for Client tsdown. The Agent/Session lookup policy lives in <code>@deepseek-ai/dsh-api-session-controller</code>, not in <code>api-remotes</code>.</p><p>Each contributing business package writes generated files to its own <code>lib/</code> directory, not to its source directory:</p><table tabindex="0"><thead><tr><th>File</th><th>Consumer</th><th>Contents</th></tr></thead><tbody><tr><td><code>typert.host.js</code></td><td>Host Loader</td><td>Runtime reflection for the Host face, strict invocation descriptors, and schema registration values</td></tr><tr><td><code>typert.host.d.ts</code></td><td>Host type system</td><td>Generated declarations for the Host face</td></tr><tr><td><code>typert.remote-client.js</code></td><td><code>api-remotes</code></td><td>A mountable <code>TypertRemoteContribution</code> containing strict descriptors and runtime codecs</td></tr><tr><td><code>typert.remote-client.d.ts</code></td><td>Client type system</td><td>Declaration merges for <code>TypertRemoteNamespaceMap</code> and <code>TypertRemoteScopeMap</code>, plus Client-safe type references</td></tr><tr><td><code>typert.remote-client.d.ts.map</code></td><td>Editor</td><td>Maps generated method properties back to Remote method declarations in the Host package</td></tr></tbody></table><p>Business packages expose the Host Loader entry through <code>./typert</code> and the Host-for-Client entry through <code>./remote</code>. The generator also validates these package exports and published-file lists; it generates artifacts only for explicit contribution packages that provide the corresponding entry.</p><p>Parameter names in Remote Client declarations come from wire fields, while parameter and return types reference Client-safe types exported by the original business package. The declaration map resolves the generated property behind <code>ctx.remote.goals.create</code> back to the Host source method marked with <code>@Remote</code>, so editors that support declaration maps can navigate from a Client call to the real implementation instead of stopping at the generated <code>.d.ts</code>.</p><p>Strict analysis requires a Remote to be a public, non-static instance method with a concrete implementation. The method cannot be generic; parameters must be required, named simple identifiers and cannot use destructuring, default values, rest parameters, or optional parameters. Typert generates strict schemas for ordinary JSON-representable types; complex objects such as workspace classes must have a unique <code>TypertLookupMap</code> declaration. Lookup and Context packages are responsible for both static declaration merges and runtime provider registration; if either side is missing, the build fails or the first call that needs the provider fails.</p><h2 id="runtime-invocation" tabindex="-1">Runtime invocation <a class="header-anchor" href="#runtime-invocation" aria-label="Permalink to &quot;Runtime invocation&quot;">​</a></h2><p>Remote calls use the Connection&#39;s <code>/api</code> route. The Client Remote calls <code>connection.rpc.call(&#39;/api&#39;, &#39;&lt;namespace&gt;/&lt;method&gt;&#39;, { args }, signal)</code>; the HTTP carrier maps this to <code>POST /api/&lt;namespace&gt;/&lt;method&gt;</code>, with a payload containing only a named <code>args</code> object.</p><p>The Connection performs the unified trust check for <code>/api</code> before the HTTP bridge, then dispatches inside the shared FetchHandler. The Typert Gateway claims only two-segment endpoints that have a strict descriptor or active SRC marker; feature-owned exact Fetch routes handle non-JSON responses, and other requests return 404. The Connection owns transport, RPC ids, response envelopes, and request cancellation, while the Gateway owns only the Remote data protocol and business dispatch. Replacing the Connection carrier does not require changes to Remote descriptors or the Client programming interface.</p><p>For every call, the Gateway resolves the descriptor and live service from the current registries instead of caching business objects. It requires the fields in <code>args</code> to match the descriptor exactly, validates wire values with codecs, resolves objects or receivers through registered lookup or Context providers, invokes the service method targeted by the binding, and validates the return value. A missing provider, unknown identity, binding mismatch, missing or extra argument, schema failure, or missing method fails before entering or after leaving business code.</p><p>The lookup provider&#39;s <code>register()</code> supplies both the stable declaration and the default resolver; <code>configure()</code> supplies a resolver owned by Host composition that may execute asynchronously and is scoped to an effect lifetime. Configuration may precede provider mounting; without a provider, invocation still fails with <code>gateway/lookup-unavailable</code>, and unloading the configuration restores the provider&#39;s default policy. The Session Controller owns the standard resolver semantics for <code>agent</code> and <code>session</code>: it reuses a live Agent, automatically resumes ordinary cold sessions, deduplicates concurrent resumes, and rejects identities owned by subagent routing; the <code>session</code> lookup returns that Agent&#39;s Session. A resume failure and an ownership fence raise a <code>RemoteError</code> carrying their own code, <code>session/not-found</code> or <code>session/agent-busy</code>, which the Gateway encodes onto the wire unchanged; only an unclassified throw folds into <code>gateway/internal</code>.</p><p>Unloading a Client contribution removes its descriptors and concrete methods together, aborts its in-flight calls, and makes stale method handles retained by external code reject further calls. A strict endpoint withdrawn on the Host also does not degrade to SRC inference, preventing a hot unload from silently weakening validation.</p><h2 id="src-development-fallback" tabindex="-1">SRC development fallback <a class="header-anchor" href="#src-development-fallback" aria-label="Permalink to &quot;SRC development fallback&quot;">​</a></h2><p>When the Host starts from source through <code>node --import tsx/esm</code>, it does not execute the Typert compiler plugin. Standard decorator initializers still record the method name and invocation mode in a versioned descriptor on the Service prototype, while <code>TypertRemoteService</code> or <code>bindTypertRemote()</code> supplies the explicit service binding; the Gateway can therefore construct a weaker temporary descriptor without starting a <code>ts.Program</code>. The descriptor&#39;s stable string property name lets <code>remoteMethods()</code> read markers written by another installed copy of the protocol package.</p><p>The SRC fallback parses simple parameter names from the live function. When a parameter name matches the <code>parameter</code> of a registered lookup, such as <code>agent</code> or <code>session</code>, it uses the lookup&#39;s <code>agentId</code> or <code>sessionId</code> wire field and resolves the object on the Host; other parameters are checked only for cycle-free, JSON-safe data with no special prototype. <code>@RemoteScope</code> directly uses the wire field of a registered Host Context provider. SRC does not read TypeScript types, generate Zod schemas, infer optional parameters, or support destructuring, default values, rest parameters, or duplicate parameter names.</p><p>SRC solves only dispatch for a Host process running from source. The Client does not discover decorators from the running Host, and the Client Remote refuses to mount SRC descriptors that lack strict codecs; its types, codecs, and Remote registration values always come from the most recently generated <code>lib/typert.remote-client.*</code> artifacts.</p><h2 id="development-mode" tabindex="-1">Development mode <a class="header-anchor" href="#development-mode" aria-label="Permalink to &quot;Development mode&quot;">​</a></h2><p>Web development prepares current Host, Client, and Web artifacts with <code>pnpm run build</code>, then runs the source Host and the Client plugin watcher in separate terminals:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dsh</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> web</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev:web</span></span></code></pre></div><p><code>dsh</code> starts the Host source through tsx, so the Host can use the SRC fallback; <code>dev:web</code> watches only Client plugins with a <code>dsh.client</code> declaration and rewrites their <code>lib/client.js</code>. It does not analyze Host decorators or generate Remote Client DTS.</p><p>Changing only a Remote method&#39;s implementation body without changing its contract does not require regenerating the Typert files. After adding or removing a decorator or changing an export name, namespace, parameter, return value, lookup, Context, or cancellation signature, rerun the ordered lib build so the Host generates the strict contract before the Client compiles and bundles the new contribution:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build:lib</span></span></code></pre></div><p>The running Client watcher consumes these generated files when it rebundles. If <code>pnpm run build:lib:host</code> has already refreshed the Host contract, <code>pnpm run build:lib:client</code> can complete the Client side; a clean worktree cannot skip the Host phase. Recompiling only the frontend source cannot infer new types from Host decorators. <code>pnpm run typecheck</code> runs the Host lib phase before Client tsc, and CI and release builds use the same order.</p><h2 id="boundaries" tabindex="-1">Boundaries <a class="header-anchor" href="#boundaries" aria-label="Permalink to &quot;Boundaries&quot;">​</a></h2><p>Remote handles only unary method calls with one request and one result. Session event streams, pagination, incremental reduce, projection, and entity substreams require a separate data protocol and registration model; even when they reuse the Connection, they must not masquerade as Remote methods or enter invocation descriptors.</p><p>The API layers are organized as <code>remotes → gateway → connection → webserver</code>. The BFF and Typert RPC layers live under <code>packages/api</code>; Connection and WebServer live at <code>packages/client/connection</code> and <code>packages/host/webserver</code>. A feature that needs a streamed or browser-native response registers an exact Connection Fetch route instead of defining a Remote method.</p><p>Lookup policy is configured per key, so all <code>agent</code> or <code>session</code> parameters share the cold-resume behavior. Accepting live objects only would require an explicit per-parameter or per-endpoint policy, which does not exist; the business method must not guess whether the object came from restoration.</p>`,46)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
