{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "extensions-window",
  "description": "Local extension preflight document; no remote origin receives native permissions.",
  "windows": ["extensions"],
  "permissions": ["core:default"]
}
