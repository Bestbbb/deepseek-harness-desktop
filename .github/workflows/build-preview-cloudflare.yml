name: Upstream Cloudflare preview (disabled)

# This distribution does not own DeepSeek's private runner or Cloudflare project.
# Website publication requires an explicitly configured community destination.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Upstream Cloudflare preview is disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "The community repository does not deploy to the upstream Cloudflare account. Documentation builds remain in CI."
